import React from 'react';
import PropTypes from 'prop-types';

import './Footer.css';

const Footer = props => {
	return (
		<div className="footer">	
			This is the footer
		</div>
	);
};

Footer.propTypes = {
	
};

export default Footer;