import React from 'react';
import PropTypes from 'prop-types';

import './Body.css';

const Body = props => {
	return (
		<div className="body">
			This is the body
		</div>
	);
};

Body.propTypes = {
	
};

export default Body;